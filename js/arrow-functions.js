/*
 * here is multiple ways you can write functions
 * */

function foo(input) {
  return input + 2
}
const bar = (input) => { return input + 2 }
const baz = (input) => input + 2
const foobar = input => input + 2
console.log(baz(2))
